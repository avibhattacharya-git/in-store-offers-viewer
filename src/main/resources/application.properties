# Application Name
spring.application.name=offers-viewer

# Active Profile (default to dev)
spring.profiles.active=dev

# Server Configuration
server.port=8080

# JPA Configuration
spring.jpa.open-in-view=false
